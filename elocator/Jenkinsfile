pipeline {
  agent any

  tools {
   // maven "jenkin-mvn"
    maven 'M2_HOME' 
    jdk 'JAVA_HOME' 
  }  
 
  stages {
    	stage ("Build") {
            steps 
            {
                sh "mvn clean package -Dmaven.test.skip=true"
            }            
        }   
     } 
     
     post {
          always {
                    cleanWs()
                 }
          }
	  
}