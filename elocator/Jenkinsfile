pipeline {
  agent any

  tools {
    maven 'jenkin-mvn'
  }  
 
  stages {
  
	    
   stage('Build') {
      steps {
        echo "PATH = ${PATH}"
        echo "M2_HOME = ${M2_HOME}"
      }
    }
    
    
  } 
	  
}